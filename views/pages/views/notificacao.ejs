<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <title>Tela de Notificações</title>
    <style>
            body {
              background: #fff;
              font-family: Arial, sans-serif;
              margin: 0;
              padding: 0;
            }
            .header {
              background: #222;
              color: #b3e5fc;
              padding: 18px 32px;
              font-size: 1.5rem;
              font-weight: 600;
            }
            .main-container {
              margin: 24px auto;
              max-width: 1100px;
              background: #fff;
              border-radius: 8px;
              padding: 18px 12px 32px 12px;
            }
            .barra {
              background: #6c63ff;
              height: 36px;
              border-radius: 20px 20px 20px 20px;
              margin-bottom: 0;
              display: flex;
              align-items: center;
              padding-left: 16px;
              color: #fff;
              font-weight: 600;
              font-size: 1.1rem;
            }
            table {
              width: 100%;
              border-collapse: collapse;
              margin-top: 0;
              background: #fff;
            }
            th,
            td {
              padding: 16px 12px;
              text-align: left;
              border-bottom: 1px solid #444;
              font-size: 1rem;
            }
            th {
              background: #6c63ff;
              color: #fff;
              border: none;
              font-weight: 600;
            }
            tr:last-child td {
              border-bottom: none;
            }
            @media (max-width: 700px) {
              .main-container {
                padding: 4px;
              }
              th,
              td {
                padding: 8px 4px;
                font-size: 0.95rem;
              }
            }
          <!DOCTYPE html>
      <html lang="pt-br">
        <head>
          <meta charset="UTF-8" />
          <title>Tela de Notificações</title>
          <style>
            /* ...existing styles... */
    </style>
  </head>
  <body>
    <div class="header">Tela de Notificações</div>
    <div class="main-container">
      <% if (typeof booking !== 'undefined' && booking.conflictMsg) { %>
      <div style="color: red; margin-bottom: 16px">
        <%= booking.conflictMsg %>
      </div>
      <% } %>
      <table>
        <thead>
          <tr>
            <th>Data</th>
            <th>Horário</th>
            <th>Sala</th>
            <th>Descrição</th>
          </tr>
        </thead>
        <tbody>
          <% if (typeof booking !== 'undefined' && booking.data) { %>
          <tr>
            <td><%= booking.data %></td>
            <td><%= booking.horario %></td>
            <td><%= booking.sala %></td>
            <td><%= booking.descricao %></td>
          </tr>
          <% } else { %>
          <tr>
            <td colspan="4">Nenhuma reserva encontrada.</td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>
