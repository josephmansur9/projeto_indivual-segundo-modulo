<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <title>Tela de Cadastro</title>
    <style>
      body {
        background: #fff;
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
      }
      .header {
        background: #222;
        color: #4fc3f7;
        padding: 18px 32px;
        font-size: 1.5rem;
        font-weight: 600;
      }
      .main-container {
        margin: 32px auto;
        max-width: 1100px;
        min-height: 90vh;
        padding: 24px;
        border: 3px solid #4fc3f7;
        border-radius: 8px;
        background: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .cadastro-tablet {
        background: #6c63ff;
        border-radius: 24px;
        width: 420px;
        padding: 40px 32px;
        box-shadow: 0 2px 24px #0002;
        display: flex;
        flex-direction: column;
        align-items: center;
        position: relative;
        z-index: 2;
      }
      .avatar {
        background: #fff;
        border-radius: 50%;
        width: 70px;
        height: 70px;
        margin-bottom: 24px;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .avatar-icon {
        width: 40px;
        height: 40px;
        fill: #6c63ff;
      }
      .cadastro-form {
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: 16px;
      }
      .cadastro-form label {
        color: #fff;
        font-size: 1rem;
        margin-bottom: 4px;
      }
      .cadastro-form input {
        padding: 10px;
        border-radius: 8px;
        border: none;
        font-size: 1rem;
        margin-bottom: 8px;
      }
      .cadastro-form button {
        background: #222;
        color: #fff;
        border: none;
        border-radius: 10px;
        padding: 12px;
        font-size: 1.1rem;
        cursor: pointer;
        margin-top: 8px;
        transition: background 0.2s;
      }
      .cadastro-form button:hover {
        background: #444;
      }
      .illustration {
        margin-left: 40px;
        z-index: 1;
      }
      @media (max-width: 900px) {
        .main-container {
          flex-direction: column;
          align-items: center;
          padding: 10px;
        }
        .illustration {
          display: none;
        }
      }
    </style>
  </head>
  <body>
    <div class="header">Tela de Cadastro</div>
    <div class="main-container">
      <div class="cadastro-tablet">
        <div class="avatar">
          <svg class="avatar-icon" viewBox="0 0 64 64">
            <circle cx="32" cy="24" r="16" fill="#6c63ff" />
            <ellipse cx="32" cy="48" rx="20" ry="12" fill="#6c63ff" />
            <circle cx="32" cy="24" r="12" fill="#fff" />
          </svg>
        </div>
        <form class="cadastro-form" action="/users" method="POST">
          <label for="name">Nome</label>
          <input type="text" id="name" name="name" required />

          <label for="id_curso">Curso</label>
          <select id="id_curso" name="id_curso" required>
            <option value="1">Engenharia Civil</option>
            <option value="2">Ciência da Computação</option>
          </select>

          <label for="lastname">Senha</label>
          <input type="password" id="lastname" name="lastname" required />
          <button type="submit">Cadastrar</button>
        </form>
      </div>
      <div class="illustration">
        <!-- Você pode adicionar uma imagem SVG ou PNG aqui para ilustrar, se desejar -->
      </div>
    </div>
  </body>
</html>
